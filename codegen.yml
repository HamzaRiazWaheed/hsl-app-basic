schema: https://api.digitransit.fi/ # Replace with your GraphQL API endpoint
documents: src/**/*.graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
